# Default values for .helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  namespace: django-app-dev

database: dev

image:
  repository: ghcr.io/dinikon/quickstart-django-dev

config:
  debug: "True"
